# analytics-experiments

Repository for experiment **analysis artifacts** (tables, notes, plots) kept separate from the `mec-streaming-framework` codebase.

## Contents

- `baseline_comparison/` — consolidated baseline aggregates (full_mec vs cloud_only).

Raw run outputs remain under `mec-streaming-framework/runs/` (typically gitignored there).
